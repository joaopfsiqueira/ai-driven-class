# Vite proxy target used in development
VITE_API_PROXY_TARGET=http://localhost:3001

# Optional direct URL (requires backend with CORS enabled)
# VITE_API_BASE_URL=http://localhost:3001

# Optional static token for quick tests
# VITE_API_TOKEN=
